# Prometheus mounts this directory read-only. Put the production scrape token in
# a file called `metrics-token` here — no trailing newline, same value as
# METRICS_TOKEN on the deployment. The directory is gitignored.
